import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';

const IconNavbar = ({iconName}) => {
  return (
    <Icon name={iconName} size={20} color="white" />
  );
};


export default IconNavbar;
